{
  "name": "dental-clinic-app",
  "version": "1.0.0",
  "description": "Dental Clinic Management Application - Single Command Deployment",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "postinstall": "npm run install-all",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run prisma:generate && npm run build --prefix frontend && npm run build --prefix backend",
    "start": "npm run start --prefix backend",
    "prisma:generate": "npm run prisma:generate --prefix backend",
    "prisma:migrate": "npm run prisma:migrate --prefix backend",
    "prisma:studio": "npm run prisma:studio --prefix backend",
    "seed": "npm run seed --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
